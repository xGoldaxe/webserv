if method == post
	if content-type && content-type == multipart/ form-data;
	{
		try
			store body;
			read and process the body;
			if filename precised
				store the content in DOWNLOAD/filename.dl.staticnb.timestamp
			send post page
		catch
			error 400
	}